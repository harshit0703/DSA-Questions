#include<bits/stdc++.h>
using namespace std;

int minX(vector<int> &arr, int n){

    int st = 1 - arr[0];
    int sum = 0;

    for(int i = 0; i < n; i++){

        sum += 

    }

}

int main()
{
    int n; cin>>n;
    vector<int> arr;
    for(int i = 0; i < n; i++){
        int temp;
        cin>>temp;
        arr.push_back(temp);
    }

    int ans = minX(arr, n);

    cout<<ans;

    return 0;
}